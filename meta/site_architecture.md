# site_architecture.md

## 概要

CCT Lab は、**生活のリズムと状態を整える科学メディア**である。  
睡眠・食事・運動・呼吸・姿勢・感情・認知・環境・習慣などを、研究知見から日常へ翻訳し、再現可能な生活実践へつなげることを目的とする。

このサイトは医療行為や診断を行うものではなく、研究・観察・実践のための個人研究ラボとして運営する。  
記事は、科学研究を起点にしつつ、生活の中で起きる変化や感覚を整理し、読者が「今日の1アクション」に落とし込めることを重視する。

---

## SSOT方針

このファイルは、CCT Lab 全体の**単一の正本（SSOT）**である。  
サイト構造、編集方針、AI生成方針、運用方針に関する最上位の定義はここに集約する。

### ルール
- 類似目的の定義ファイルを増やさない
- 構造変更や運用変更が発生した場合は、まずこのファイルを更新する
- 他のプロンプトや補助ファイルは、このファイルと矛盾してはならない
- 生成系AIに複数の断片的ルールを渡すのではなく、このファイルを軸に整合を取る

### 実行時の扱い
- このファイルは **SSOT ではあるが、AI実行時に毎回フルで渡す前提ではない**
- 実行時にAIへ渡す定義は、役割分担された実行用ファイルに集約する
- 実行時にどのファイルをAIへ渡すかは、下記「AI実行時に渡す定義ファイル」で定義する

---

## サイトの目的

CCT Lab の目的は、以下を読者に提供すること。

1. 眠気・だるさ・食欲・集中力・気分などを「状態」として理解する視点
2. 原因を単一化せず、睡眠・血糖・食行動・活動量・刺激量・呼吸・姿勢などの相互作用として捉える視点
3. 日常の不調を、観察 → 整理 → 小さな実践 へ変換する足場
4. 「やる気」を根性論ではなく、身体と生活リズムの結果として扱う考え方

---

## ブランドと基本表現

### ブランド名
CCT Lab

### タグライン
生活のリズムと状態を整える科学メディア

### 中核概念
CCT（Core Cycle Tune）は、眠気・だるさ・食欲・集中力・気分などの状態を観察し、不協を見つけ、食事・睡眠・運動を通じた解決を重ねながら、日々の調子を少しずつ調律していくメソッドである。

### 用語の統一
以下3語は常にこの表記で統一する。

1. コアサイクルチューン（循環調律）
2. 不協（ディゾナンス）
3. 解決（レゾリューション）

### 固定文
記事内で不協の話を始める前には、以下の固定文を優先的に置く。

コアサイクルチューン（循環調律）では、生活の好循環を乱す要素を不協（ディゾナンス）、それを整える行動を解決（レゾリューション）として扱います。

### 見出しルール
記事の見出しには、原則として以下の用語を直接使わない。

- コアサイクルチューン（循環調律）
- 不協（ディゾナンス）
- 解決（レゾリューション）

見出しは、**その節の内容をSEO語句で要約した一文**にする。  
章ラベルやメタ見出しにしない。

禁止例：
- 研究が示すこと
- 日常で何が起きているか
- 東洋医学の補助的な見方
- まとめ

---

## 編集方針

### 記事の基本思想
記事は、単なる健康Tipsではなく、**研究知見を生活実感に翻訳する読み物**として設計する。

### 基本の流れ
記事は原則として次の流れを取る。

引用  
↓  
研究知見  
↓  
日常理解  
↓  
循環理解  
↓  
生活実践  
↓  
研究詳細の整理

### 文体
- 基本はです・ます調
- 強すぎる断定を避ける
- 研究の限界や条件差を認める
- それでも読後に「やってみる理由」が残るように書く
- 科学寄りだが冷たすぎず、生活者の感覚に寄り添う

### 研究の扱い
- 研究を起点にする
- 研究を権威づけの飾りにしない
- 論文の一節や要点は、導入のフックとして活用する
- 面白さ優先でテーマを選んでよいが、必ず研究ベースに接続する
- Research Note は本文導線を止めないよう、**末尾に置く**

### 東洋医学の扱い
- 東洋医学は主軸ではなく補助線として使う
- 薄い一般論にせず、文脈に応じて適切な東洋医学用語を織り交ぜる
- 使いうる語の例：気滞、脾胃、湿、痰湿、心神不寧、肝気鬱結
- 現代医学・生理学と対立させず、読者理解を厚くする補助的視点として扱う

### 禁止・非推奨
- 「もちろん主軸は現代の〜研究ですが、」という逃げの定型文は禁止
- AIっぽい過剰な万能感、完成宣言、誇張表現は避ける
- 医療断定、治療断定、病名の安易な一般化は避ける

---

## カテゴリとトピック

### カテゴリ
現行の大分類は以下を基本とする。

- 睡眠・休養
- 食事・栄養
- 運動・活動
- 呼吸・姿勢
- 感情・認知
- 環境・習慣
- 社会・つながり

必要に応じてサイト運用上のカテゴリを増減してもよいが、乱立は避ける。

### トピック
トピックは、カテゴリよりも具体的な関心軸を示す補助ラベルである。  
検索導線、関連記事導線、テーマの偏り把握に使う。

### タグ
タグは、記事内容の補助的な文脈ラベルとして扱う。  
細かすぎる乱立は避けるが、本文内容と検索意図に沿う範囲で付与する。

---

## 現在のサイト構造

静的サイトとして運用し、記事ソースからビルド成果物を生成する。

### 基本構造
- `articles_src/<slug>/article.md`
- `articles/<slug>/index.html`
- `data/posts.json`
- `templates/article.html`
- `assets/app.js`
- `assets/style.css`

### AIレビュー用構造
- `articles_draft/<slug>/article.md`
- `tmp/ai_generation_summary.json`

### 既存フロー
1. `articles_src/<slug>/article.md` を作成
2. HTMLへビルド
3. `data/posts.json` を生成
4. `sitemap.xml` を生成
5. 公開反映

### AIレビュー時フロー
1. AIが `articles_draft/<slug>/article.md` を生成
2. 内容確認
3. 必要なら人手修正
4. 正式版を `articles_src/<slug>/article.md` に置く
5. 既存ビルドへ接続

### front matter 必須項目
原則として以下を含む。

- title
- description
- updated
- tags
- topics
- category
- readingTime
- eyecatchQuery

必要に応じて eyecatch 関連フィールドを後段で付与する。

---

## 記事本文の必須構造

CCT Lab の記事本文には、以下の構造体を必須とする。

1. `[quote] ... [/quote]`
2. `[cct-cycle type="dissonance"] ... [/cct-cycle]`
3. `[cct-cycle type="resolution"] ... [/cct-cycle]`
4. `Research Note`
5. `[paper-summary] ... [/paper-summary]`

### 配置順
本文は原則として次の順にする。

1. front matter
2. `[quote] ... [/quote]`
3. 導入
4. 研究の要点
5. 日常で何が起きているか
6. 循環理解
7. `[cct-cycle type="dissonance"] ... [/cct-cycle]`
8. 解決の方向性
9. `[cct-cycle type="resolution"] ... [/cct-cycle]`
10. 必要に応じて東洋医学の補助的理解
11. まとめ
12. `Research Note`
13. `[paper-summary] ... [/paper-summary]`

### 重要
- `Research Note` は **本文末尾**
- `[paper-summary]` も **本文末尾**
- 冒頭に置かない
- HTML化しても並び替えないため、Markdown 上の順番がそのまま反映される

---

## AI生成の基本方針

CCT Lab のAI生成は、**構造より先にプロンプトを強くする**方針を取る。

### 基本原則
- まずはプロンプトで解決できることを最大限プロンプトで担う
- 構造改修やロジック追加は最小限にとどめる
- 不足が出た箇所だけ後から実装で補う
- 出力の正本は常に `articles_src/<slug>/article.md` とする

### AIの主な責務
AIは以下を担う。

- テーマ選定
- 研究ベースの記事構成
- front matter の生成
- タイトル、description、slug、eyecatchQuery の生成
- 既存記事との重複回避の配慮
- categories / topics / tags の整理
- 東洋医学補助パートの自然な挿入
- 既存文体・世界観への整合
- 必須構造体の出力

### 実装初期の考え方
「高度なことは後回し」に寄せすぎず、以下もまずはプロンプト責務として初期から狙ってよい。

- 既存記事とのテーマ重複回避
- タグの最適化
- トピックの選定
- 研究観点の選定
- SEOを意識したタイトルとdescription
- 自然言語の依頼から記事化する変換

### モデル方針
- `gpt-5.4-mini` は低コストで試行しやすいが、構造遵守や記事品質で弱さが出ることがある
- `gpt-5.4` は、見出し、Research Note、CCT構造ブロック、本文の自然さで改善が見られた
- 今後の本番生成の基準モデルは、**基本的に `gpt-5.4` を優先**する
- 低コスト試験や配線確認では `gpt-5.4-mini` を使ってもよい

---

## AI実行時に渡す定義ファイル

AI実行時に渡す定義ファイルは、以下の3つを必須とする。

1. `generate_article.md`
2. `article_structure.md`
3. `base_cct_concept.md`

### 重要
- この3つは **実行時必須**
- ただし **役割は重複させない**
- `base_cct_concept.md` は必須
- `site_architecture.md` は SSOT だが、実行時には毎回渡さない

---

## 定義ファイルの責務分担

### `site_architecture.md`
- サイト全体のSSOT
- 構造、運用、編集思想、AI方針の最上位定義
- 実行時投入前提ではない

### `base_cct_concept.md`
- CCT Lab の世界観、概念、言葉の軸
- コアサイクルチューン（循環調律）の思想
- 不協（ディゾナンス）/ 解決（レゾリューション）の理解の軸

### `article_structure.md`
- 記事の並び順
- 構造ブロックの必須性
- 見出し設計
- 固定文の位置
- 本文構成の役割分担

### `generate_article.md`
- 実行用プロンプト本体
- 出力形式
- front matter 仕様
- AIへの具体的指示
- 構造体の必須指定
- 末尾配置などの最終出力ルール

---

## AI生成ワークフロー

### 出力形式
AI生成でも手動作成でも、最終的な出力先は同じとする。

- 正式版: `articles_src/<slug>/article.md`
- レビュー版: `articles_draft/<slug>/article.md`

### 基本フロー
1. テーマ決定
2. AIが `article.md` を生成
3. 形式・最低限の妥当性を確認
4. モードに応じてレビュー止まりか公開まで進める
5. 既存のビルド・一覧生成・sitemap生成へ接続

### モード制御
AI運用にはワークフローモードを持たせる。

例：
- `review`
- `auto_publish`

意味：
- `review`  
  自動生成＋確認用出力。生成物は `articles_draft` に置く。自動公開はしない。
- `auto_publish`  
  自動生成＋公開まで進める。生成物は `articles_src` に置き、ビルドまで接続する。

この制御は、単一の定数または設定値で切り替え可能にする。

### 安全原則
- モードが `auto_publish` でも、明らかに壊れた出力は止める
- 初期段階の検証は「最低限の壊れチェック」に留める
- 文章品質やSEO品質の完全自動判定は後回しでよい
- ただし、必須構造体の欠落は止める

---

## generate_ai_article.js の役割

`generate_ai_article.js` の本来の役割は、**AI呼び出しと受け渡し**である。

### 主な責務
- OpenAI API を呼ぶ
- `slug` と `article_md` を受け取る
- 保存先フォルダを作る
- `article.md` を保存する
- 最低限の壊れチェックを行う
- 要約ファイルを出力する

### 最低限の壊れチェック
現時点で持たせる責務は、以下のような**最低限の構造チェック**に留める。

- front matter がある
- 必須 front matter キーがある
- 本文が空でない
- `[quote] ... [/quote]` がある
- `Research Note` がある
- `[paper-summary] ... [/paper-summary]` がある
- `[cct-cycle type="dissonance"] ... [/cct-cycle]` がある
- `[cct-cycle type="resolution"] ... [/cct-cycle]` がある
- slug が空でない
- slug が重複しない

### 持たせすぎない責務
以下は、現時点では `generate_ai_article.js` の責務としない。

- 記事品質の精密評価
- 見出しのSEO品質判定
- 東洋医学パートの厚み判定
- 完全自動の採点
- 高度な再試行戦略

---

## 検証実装の方針

### 判定思想
独自構造ブロックは、**コードで判定可能**であり、判定すべきである。

### 実装方針
- `regex` で不安定な挙動を見せた箇所は、必要に応じて `indexOf` / `includes` ベースへ寄せる
- 特に以下は、見た目に存在するのに `regex` が false になる事象が実際に発生した
  - `[quote] ... [/quote]`
  - `[paper-summary] ... [/paper-summary]`
- そのため、実務上は **開きタグ・閉じタグ・順序** を見る素直な判定を優先する

### ログ方針
- 初期デバッグ時には、生成本文全文ログを一時的に許容する
- 原因切り分けが終わったら、過剰ログは減らしてよい
- エラー原因は、どの構造が足りないのかが一目で分かるメッセージにする

---

## 自動テーマ生成と手動記事依頼

将来的に、AI記事生成の入口は2系統持てる設計にする。

### 1. 自動入口
システムが既存記事群やテーマバランスを見て、自動で記事テーマを決める。

### 2. 手動入口
運営者が自然言語で「こんな記事を書いて」と依頼できる。

例：
- 最近、朝散歩と血糖値の関係が気になる。研究ベースで記事にして。
- 週末に娘と過ごすと生活リズムが整う感覚を、家庭のリズムと自律神経の観点で記事化したい。

### article request 概念
手動依頼は、将来的に内部では article request として扱える設計にする。  
ただし初期段階では、実装より先に「そういう入口を持てる構造思想」を定義しておけばよい。

---

## 現時点の運用メモ

### 成果
- AI生成 → 構造チェック → `articles_draft` 保存 は成立した
- `gpt-5.4` にすると記事品質がかなり改善した
- `gpt-5.4-mini` は構造遵守や品質で弱さが見られた

### 確定事項
- Research Note は末尾に置く
- HTML側では並び替えない
- Markdown の順番がそのまま出る
- 実行時に渡す定義ファイルは3つ必須
- `generate_ai_article.js` は橋渡し役＋最低限検証に留める

### 正式化済み候補
以下のように、`articles_draft` から内容確認後に `articles_src` へ正式化する運用を認める。

- `articles_src/green-exercise-mood/article.md`
- `articles_src/social-jetlag-weekend-fatigue/article.md`

---

## 今後の改善余地

今後必要に応じて検討するもの。

- 見出し品質の改善
- Research Note の研究実在感の強化
- モデル比較の継続
- `review` と `auto_publish` の運用整理
- 成果物の差分確認導線の改善
- ログ出力の整理
- prompt のさらなる責務調整

ただし、**まずはSSOTと実行プロンプトの整合を保つこと**を優先する。

---

## GitHub Actions の運用分岐

CCT Lab では、**あなたのコミット→プッシュ** と、**その後に GitHub Actions が何をするか** を分けて考える。

### 用語
- あなたのコミット→プッシュ  
  スマホから行う通常の更新操作。実務上は1セットとして扱う。
- AI生成  
  `scripts/generate_ai_article.js` が実行され、OpenAI API を呼ぶこと。これが走ると課金対象になる。
- ビルド  
  記事HTML、`data/posts.json`、`sitemap.xml` などの公開用生成物を作ること。
- Actions側の自動反映  
  Actions が生成したファイルをさらにコミット→プッシュすること。

### 基本原則
- **AI生成は、記事を生成したいときだけ動かす**
- 通常の構造変更やCSS変更で、AI生成を走らせない
- `scripts/generate_ai_article.js` が実行されなければ、OpenAI API は呼ばれず、課金は発生しない
- `review` では AI生成のみを行い、Actions側の自動反映はしない
- `auto_publish` では AI生成後にビルドし、必要なら Actions側の自動反映を行う
- push 起点の通常ビルドでは、AI生成を走らせない

---

## ファイル分類と Actions の挙動

### 1. 記事を手動で追加・修正
対象:
- `articles_src/**`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: する
- Actions側の自動反映: する

### 2. テンプレートやビルドロジックを変更
対象:
- `templates/**`
- `scripts/build_article.js`
- `scripts/generate_posts.js`
- `scripts/generate_sitemap.js`
- `scripts/fetch_eyecatch.js`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: する
- Actions側の自動反映: する

### 3. CSS / JS のみを変更
対象:
- `assets/style.css`
- `assets/app.js`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: 原則しない
- Actions側の自動反映: しない

補足:
- `app.js` は公開後のページで動くフロント用JSであり、主に共通UI、パンくず、`posts.json` を元にした一覧・関連記事描画などを担う
- 記事HTMLそのものを生成するスクリプトではないため、単独変更では原則ビルド対象にしない

### 4. AI用の定義ファイル・プロンプトを変更
対象:
- `generate_article.md`
- `article_structure.md`
- `base_cct_concept.md`
- `site_architecture.md`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: しない
- Actions側の自動反映: しない

補足:
- これらは次回のAI生成ルールにだけ影響する
- 既存公開ページを直接変えない

### 5. workflow 自体を変更
対象:
- `.github/workflows/generate-posts.yml`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: しない
- Actions側の自動反映: しない

### 6. AI生成スクリプトを変更
対象:
- `scripts/generate_ai_article.js`

挙動:
- あなたのコミット→プッシュ: する
- AI生成: しない
- ビルド: しない
- Actions側の自動反映: しない

補足:
- これは AI生成ロジックの変更であり、通常 push では実行しない
- 次回 review / auto_publish / schedule 実行時にだけ効く

### 7. AIレビュー用の生成物
対象:
- `articles_draft/**`
- `tmp/ai_generation_summary.json`

挙動:
- 通常 push の監視対象にしない
- 公開用ビルドにも使わない
- review 実行時の確認用とする

---

## trigger ごとの挙動

### push
- AI生成: しない
- ビルド: 必要な変更に対してのみ行う
- Actions側の自動反映: 必要な変更に対してのみ行う

### workflow_dispatch
- `review`
  - AI生成: する
  - ビルド: しない
  - Actions側の自動反映: しない
- `auto_publish`
  - AI生成: する
  - ビルド: する
  - Actions側の自動反映: する

### schedule
- 原則として `review` 相当で扱う
- AI生成: する
- ビルド: しない
- Actions側の自動反映: しない

---

## 重要な安全原則

- AI生成後の push は、条件分岐を誤ると loop の原因になりうる
- そのため、**push起点では AI生成を走らせない**ことを基本原則とする
- 課金の発生源は OpenAI API 呼び出しであり、`scripts/generate_ai_article.js` が実行されない限り課金は発生しない
- review は確認用であり、main を勝手に更新しない
